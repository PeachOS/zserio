pipeline {
  agent {
    node {
      label 'linux64'
    }

  }
  stages {
    stage('Message') {
      agent {
        node {
          label 'linux64'
        }

      }
      steps {
        echo 'Starting pipeline'
      }
    }
  }
}